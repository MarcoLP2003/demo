spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://localhost:3306/nomedb
    username: root
    password: Mlp160903

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true